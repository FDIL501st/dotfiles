# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = FDIL
	email = fdil.fadilh1@gmail.com
[core]
	editor = nano
[init]
	defaultBranch = main

[merge]
    conflictstyle = zdiff3
    tool = vscode
[diff]
    algorithm = histogram
    tool = vscode
